import { FileText } from 'lucide-react';
import PlaceholderPage from '../components/PlaceholderPage';

export default function MockTests() {
  return (
    <PlaceholderPage
      title="Mock Tests"
      description="Take full-length mock tests that simulate real JEE exam conditions. Get detailed performance analysis after each test."
      icon={FileText}
    />
  );
}
