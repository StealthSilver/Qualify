import { Trophy } from 'lucide-react';
import PlaceholderPage from '../components/PlaceholderPage';

export default function Leaderboard() {
  return (
    <PlaceholderPage
      title="Leaderboard"
      description="See how you rank against other students. Compete in weekly and monthly challenges to climb up the rankings."
      icon={Trophy}
    />
  );
}
